pipeline {
    agent any
    stages {
        stage('Pull Code from Git') {
            steps {
                sh 'echo "checkout done"'            }
        }
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'npm run build'
                sh 'sudo cp -r build/* /opt/deployment/react'
            }
        }
        stage('Deploy') {
            steps {
                sh 'cd /opt/deployment/react'
                sh 'pm2 serve build 3000 --name "react-todo-app"'
            }
        }
    }
}
